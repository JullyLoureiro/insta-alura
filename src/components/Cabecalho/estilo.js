import { StyleSheet} from 'react-native'

const estilo = StyleSheet.create({
    fotoUsuario: {
        width: 40, 
        height: 40,
        borderRadius: 30
    },
    cabecalho:{
        flexDirection: 'row',
        alignItems: 'center',
        margin: 10,

    }
})

export default estilo
